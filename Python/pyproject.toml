[project]
name = "tccp-algorithm-python"
version = "0.1.0"
description = "Python implementation of the algorithms project."
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
